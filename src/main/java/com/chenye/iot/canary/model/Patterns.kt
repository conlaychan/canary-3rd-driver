package com.chenye.iot.canary.model

object Patterns {

    val DEVICE_ID: Regex = Regex(RegularExpression.DEVICE_ID)

    val IDENTIFIER: Regex = Regex(RegularExpression.IDENTIFIER)

    val NAME: Regex = Regex(RegularExpression.NAME)
}
